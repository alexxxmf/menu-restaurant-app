import { post } from "./base";

// export const login = async (values: {
//   email: string;
//   password: string;
// }): Promise<{ authorization: string }> => {
//   const response = await post("/user/login", { data: values });
//   console.log("response", response);
//   return response;
// };
